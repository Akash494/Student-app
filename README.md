# Student-APP

This is the repo to 2 tier application i.e one app and one db
Student app can be found under folder 2-tier-app/src where there is Docker file using which we can create application image.

Student-App requires one env variable which DB_LINK, link to the external or internal database.
We pass this variable as env in docker or kubernetes.
