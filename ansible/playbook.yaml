---
- hosts: localhost
  tasks:
    - name: Check whoami
      command: whoami

---
- hosts: deploy
  become: true
  tasks:
    - name: Check hostname
      command: hostname
    - name: Copy application
      copy:
        src: /tmp/random.txt
        dest: /opt/random.txt
